
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hello-demo
  namespace: argocd
spec:
  destination:
    namespace: default
    server: https://kubernetes.default.svc
  project: default
  source:
    repoURL: https://github.com/stdedwin-git/poc-version-trace.git   # <- Windows usa / o \
    targetRevision: HEAD
    path: base
    kustomize: {}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true

